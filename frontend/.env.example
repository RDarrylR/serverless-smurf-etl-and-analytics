# Frontend Environment Variables

# API Gateway Base URL from Terraform deployment
# After deploying infrastructure, copy the API Gateway URL output here
# NOTE: Do NOT include the endpoint path (e.g., /generate-upload-url or /files)
# The app will append the correct endpoint automatically

REACT_APP_API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod

# Example URLs:
# Local development (if using SAM local):
# REACT_APP_API_URL=http://localhost:3000

# Production:
# REACT_APP_API_URL=https://abc123xyz.execute-api.us-east-1.amazonaws.com/prod
